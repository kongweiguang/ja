<!-- @author kongweiguang -->
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

# 贡献指南

感谢参与 JA。项目仍在早期实现阶段，贡献前请先确认对应的持久计划任务、代码
所有权和验收边界；不要把尚未完成的设计写成已经提供的能力。

## 开始前

1. 阅读 [README](./README.md)，确认本地工具链和当前产品边界。
2. 检查当前工作区是否有未归因改动，不要覆盖、回滚、stash 或格式化其他人的修改。
3. 对跨层行为先阅读 `.updeng/docs/discovery/ja-project/` 中的设计（该目录属于
   启用 Updeng 的本地工作区），并在任务单中确认你拥有的文件范围。
4. 需要引入第三方代码、资源或依赖时，先确认许可证兼容性、来源、版本、修改范围
   和对应的 notice；不能复制 Codex 私有代码或资产。

## 本地验证

当前脚手架的最窄验证是：

```powershell
pnpm install
pnpm build
```

如果修改了 Tauri/Rust、Java sidecar、stdio 协议、数据库、Skills、MCP、打包或
权限边界，还必须运行对应任务单指定的 Rust/Java/协议/安全验证。未运行的命令不能
在提交说明中写成已通过。当前仓库没有可依赖的 CI 或发布自动化承诺；这些能力必须
在真实配置和目标平台验证后再写入文档。

## 修改约定

- 新增或修改的可注释人工维护文件应包含 `@author kongweiguang` 和适用的 SPDX 标识。
- 函数级注释说明设计原因、边界或恢复语义，不重复代码表面行为。
- 保持 Java、Rust、TypeScript 的模块边界；协议、schema、迁移、清单和共享配置
  由任务单指定的唯一 owner 修改。
- secret、访问令牌、个人路径和真实用户数据不得进入源码、测试 fixture、日志或
  issue；使用经过脱敏的最小样例。
- 行为或外部契约改变时，同步对应的业务/集成文档；只做 discovery 设计时不要把
  计划功能写成当前事实。

## Pull Request 内容

提交 PR 时请说明：

- 用户可观察的变化、非目标和影响范围；
- 修改的文件/模块以及与其他任务的依赖；
- 实际运行的验证命令、结果和未覆盖的平台；
- 数据迁移、权限、进程、网络、凭据或发布方面的风险与恢复方式；
- 新增第三方依赖的许可证、来源和 notices 状态。

目前没有强制的 commit message、DCO 或自动化机器人规则；仓库若以后新增这些规则，
会在这里以可验证的配置和文档一起更新。

## 许可证

提交到 JA 的原创代码默认按 [GPL-3.0-or-later](./LICENSE) 提供。提交者需要确认
自己有权提交这些内容，并保留必要的第三方版权和许可证声明。
