# @author kongweiguang
# SPDX-License-Identifier: GPL-3.0-or-later

[package]
name = "ja-rpc-protocol-spike"
version = "0.1.0"
edition = "2024"
authors = ["kongweiguang"]
license = "GPL-3.0-or-later"
publish = false

[dependencies]
serde = { version = "=1.0.228", features = ["derive"] }
serde_json = "=1.0.145"

[[bin]]
name = "probe-child"
path = "src/bin/probe_child.rs"

[lib]
name = "ja_rpc_protocol_spike"
path = "src/lib.rs"
