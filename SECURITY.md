<!-- @author kongweiguang -->
<!-- SPDX-License-Identifier: GPL-3.0-or-later -->

# 安全问题报告

JA 仍处于早期实现阶段，尚没有承诺的支持版本、响应时限、漏洞悬赏或安全保证。
当前代码不应被视为已经完成生产安全审计的 Coding Agent。

## 报告渠道

首选 GitHub 的 Private Vulnerability Reporting / Security Advisory。仓库公开并启用
该功能后，可从仓库的 `Security` 页面选择私密漏洞报告，或使用对应的
`/security/advisories/new` 入口。请先确认页面显示的是 `kongweiguang/ja`，不要把
敏感信息发给错误的仓库。

如果私密报告功能尚未启用：

1. 不要在公开 issue、讨论区、PR、聊天记录或提交中放置漏洞复现细节、利用代码、
   API key、token、私钥、个人数据或完整日志。
2. 只在 GitHub 提供的维护者私密联系渠道可用时发送脱敏摘要，并请求切换到私密
   Security Advisory；如果没有安全的私密渠道，先不要公开披露细节。
3. 公开 issue 只允许报告不含敏感细节的安全改进建议，不能作为漏洞细节的替代提交
   入口；可以注明“需要私密安全通道”，等待维护者配置后再补充信息。

报告至少应包含受影响的 JA 版本或 commit、运行平台、最小复现步骤、影响边界和
已采取的缓解措施。请先删除或轮换日志中的凭据；不要为了验证漏洞访问他人工作区、
生产系统或外部服务。

## Agent 特有边界

涉及 shell、文件、终端、预览、MCP、Skills、模型请求、sidecar、权限 Capability
或凭据存储的漏洞，应同时说明是否需要用户审批、是否能跨 Workspace/Thread 逃逸、
是否留下子进程/临时文件/数据库残留，以及重启后是否仍可利用。这些信息帮助我们
判断影响，不代表当前功能已经实现。

维护者确认并修复前，请不要公开漏洞细节或将其用于未授权测试。修复、披露时间和
受影响版本范围由维护者在取得足够证据后另行确认；本文件不承诺固定 SLA。
